# AutoProp Scout Pro — Coordination Contract

This repository is being worked on from more than one ChatGPT conversation. Those conversations do **not** communicate directly. This file is the shared source of truth that every build chat must read before changing AutoProp production.

## Production safety rules

1. Do not change the Railway production source, start command, scanner entrypoint, or hard qualifier rules without first reading this file.
2. Do not push experimental work directly into production. Use a feature branch and validate it first.
3. Current production must remain pinned until a replacement build passes validation.
4. Never re-enable Goblin/Demon/boosted/discount lines in the regular-line production scanner.
5. Never expose one user's PickFinder credentials/session/results to another user.
6. Never treat a locked/public PickFinder page as authenticated analytics.
7. Never display malformed records such as player=`PickFinder`, sport=`UNKNOWN`, pick=`N/A` as researched props.

## Permanent product invariants

- Regular/main lines only; no Green Goblin, Demon, boosted, discounted, or alternate lines.
- Today/tonight only where a slate is date-scoped.
- Missing required data fails closed.
- Adjustable research thresholds are allowed, but the hard locks above are not adjustable.
- If nothing qualifies, show clearly labeled Best Available near-misses rather than pretending they qualified.
- Provider-native player props may be used as the All Props universe when they are real, current, non-promotional outcomes. They must be labeled by source and may never be represented as PrizePicks unless the provider explicitly identifies PrizePicks.
- PickFinder is an optional authenticated research/verification source, not a requirement for the All Props board. If it cannot provide a reliable session or board, the product must remain usable from provider-native data.

## Current production

- Railway project: AutoProp Scout Pro
- Production service: `autoprop-live`
- Production domain: `autoprop-live-production.up.railway.app`
- Recovery baseline: `9165291ec29e5f54241e1657f4f0f5602901bfc4`
- Railway source branch: `production-stable`
- Railway start command verified 2026-09-10: `node frontdoor-clearsports.mjs` (frontdoor starts the legacy Scout server plus the Auto Scout data core and separate Apex lab).

## Active v3 branch

- Branch: `autoprop-multiuser-live-data`
- Goal: replace the single-owner architecture with per-user AutoProp accounts, per-user encrypted PickFinder sessions, isolated scan history/results/rules, corrected PickFinder login verification, live scores, and live in-game player data.
- This branch must be validated before replacing production.

## Required handoff protocol for every build chat

Before making changes:
1. Read `AUTOPROP_COORDINATION.md`.
2. Inspect the currently deployed commit/config.
3. Work on an isolated feature branch unless the change is an emergency production fix.
4. Do not overwrite another chat's branch.
5. Append a short handoff note below when changing deployment architecture or product invariants.

## Handoff log

- 2026-09-08: Cross-chat overwrite problem identified. Production was pinned to a verified regular-only baseline. New multi-user/live-data work moved to `autoprop-multiuser-live-data`.
- 2026-09-08: Multi-user v3 direction approved: every AutoProp user must connect only their own PickFinder account; live scores and live player/game data are being added.
- 2026-09-09: Claude Code session on branch `claude/pickfinder-auth-modal-fix-dle7hz`, based on `production-stable` (4a3acfa). Nothing on this branch was deployed during Claude's session.
  - Fixed the PickFinder sign-in failure where the Clerk modal backdrop intercepted pointer events and `scanner/pickfinder-v2.mjs` leaked the raw Playwright call log to the dashboard. Resilient submit (click → settle+retry → form.requestSubmit → Enter → DOM click). No CAPTCHA/2FA/verification bypass; those still fail closed.
  - `lib/safe-error.mjs` is now the ONLY place that produces backend→frontend error text. It is an allowlist: unclassified errors become generic copy. Do not reintroduce per-server `friendlyScanError` bodies that return `error.message`.
  - Added `lib/props`, `lib/filters`, `lib/scoring`, `lib/data-sources` as the universal prop/filter/score engine. One filter engine only — do not add a second filter implementation for any new page.
  - Added SportsDataIO enrichment for projections, injuries, minutes, starter/lineup context, opponent data and live status. Requires `SPORTSDATAIO_API_KEY`; unavailable feeds degrade to null, never invented data.
  - Safety branch pinned at the known-good deployed commit: `safety/known-good-9e13ab7`.
- 2026-09-09: ChatGPT takeover continued on `claude/pickfinder-auth-modal-fix-dle7hz`. Owner explicitly authorized deployment after release validation. `/props` now has an organized mobile-first All Props / Auto Prop Finder UI and both `server.mjs` and the live `server-professional.mjs` map `/props` to it. SportsDataIO runtime diagnostics discover entitlements and whether exact PrizePicks operator offers are present without returning key material or raw odds payloads.
  - Added conservative SportsDataIO player-prop normalization: only available, non-alternate OVER/UNDER outcomes with a real player, market and numeric line are eligible; missing values never become zero.
  - Added `prizePicksBoard()` as an API-first candidate source, but it is NOT enabled as the production prop universe yet. PickFinder remains the authoritative fallback until Railway runtime verification proves the configured subscription actually contains PrizePicks core offers for a league. Scout Rules continue to fail closed for any source that has not satisfied the hard locks.
  - Candidate `7ee72798d810ba3972279bd693937ca48b325b04` passed the full GitHub release-candidate workflow. `production-stable` was fast-forwarded from `4a3acfa` to the validated candidate, and this documentation commit intentionally triggers the Railway production rollout. Runtime SportsDataIO verification is configured as a one-time pre-deploy diagnostic; it prints only entitlement classifications and safe operator counts.
- 2026-09-09: Product direction updated by owner: SCOUT PRO must become a provider-native sports research platform that remains useful without PickFinder. PickFinder may stay only as an optional per-user research source when it genuinely works. The production All Props universe is being moved toward real SportsDataIO player-prop feeds, with source labeling, no fabricated PrizePicks attribution, and the same hard promotional-line exclusions.
- 2026-09-09: `chatgpt/api-first-props-fix` passed the release workflow at commit `1fc7b1a15081783e9bb8565e2344f00234e00f71`. Production was fast-forwarded to that build, the Railway start command was changed to `node server-scout.mjs`, healthcheck to `/api/health`, and this documentation commit exists to emit a normal GitHub push event so Railway refreshes the `production-stable` source snapshot.
- 2026-09-09: Claude Code session. Fixed the empty prop board. Root cause: `lib/data-sources/sportsdataio/prop-board.mjs` called `normalizePlayerPropOffers(payload, { sport })` with no `metadata`, so market/bet/outcome names could not be resolved from the numeric type IDs the player-props feed returns; `side` and `market` came out null and every outcome was dropped at the `offerFromOutcome` gate — an empty board from a feed answering 200. Added `betting-metadata.mjs`, which loads `/{sport}/odds/json/BettingMetadata` and builds the lookup maps (cached 12h per league). Strictly additive: an unfamiliar shape or failed request yields empty maps and normalization falls back to the existing payload-name path. Do not remove the `metadata` argument at that call site. `/api/props` now also returns `boardState`, and coverage rows report `metadataLoaded`, to distinguish "feed returned nothing" from "feed returned data we could not name".
- 2026-09-10: ChatGPT built the PickFinder-style provider-native research layer on isolated branch `chatgpt/pickfinder-research-layer` without modifying Claude's `scout-ui-v4.js` or the existing prop qualifiers. New `lib/autoscout/research-service.mjs` joins current The Odds API player/market context to SportsDataIO player identities and historical game logs, then computes real L5/L10/L15/L20/season, H2H and home/away research through the existing rolling analytics engine. New `apex-v2/scout-ui-v5.js` adds desktop research columns plus mobile research tiles, adjustable line and Over/Under controls, game-by-game threshold chart, historical game table, sportsbook line shop, stored line movement, projection delta and available matchup/injury/starter context. Missing/unlicensed stats stay unavailable rather than becoming fabricated zeros. `frontdoor-prod.mjs` exposes the rate-limited server-side `/api/apex/research` endpoint and switches only the injected Auto Scout shell from v4 to v5. PR #16 targets `production-stable`; promotion is allowed only after release checks and production smoke are green.
- 2026-09-10: Claude Code session, branch `claude/apex-market-project-txf4lp`, rebased onto `production-stable` (6de5e41). Not deployed.
  - Customer-facing surfaces no longer name a data vendor or expose plan limits. Cards showed "The Odds API" and, on WNBA, "ClearSports does not currently document WNBA endpoints."; the public `/api/apex/health` returned `monthlyCredits`, `maxEvents`, `maxMarketsPerEvent`, `preferredProvider` and the `*Configured` vendor flags to anyone.
  - `lib/public-sanitize.mjs` is the single choke point. `frontdoor-prod.mjs` runs it over the research response and over proxied JSON for `/api/health`, `/api/apex/health` and `/api/apex/props`. Vendor-shaped copy ("does not document X endpoints", "not included in the current subscription") is REPLACED, not scrubbed, because scrubbing the name leaves our vendor contract described in the sentence. Add new provider messages freely — they are sanitized by default rather than needing to be remembered.
  - `/api/apex/research-health` is now readiness only (`{available:boolean}`). It previously returned the provider health object publicly.
  - Owner diagnostics at `/apex/diagnostics` are deliberately NOT sanitized: credits, quota, endpoints and provider identity still belong there. Do not route them through the sanitizer.
  - Sportsbook identity is product data and survives untouched — only data-vendor identity is removed.
  - My earlier research module (`lib/research/*`) is superseded by `lib/autoscout/research-service*.mjs` + the ClearSports adapter and was NOT carried forward. It is parked at `claude/research-pipeline-reference` for reference only.
- 2026-09-10: Claude Code session, branch `claude/apex-market-project-txf4lp` (on `production-stable` 6de5e41). Not deployed.
  - **Market mapping was silently unmapping ~50% of the live board.** The odds feed sends "Reception Yards" (not "Receiving Yards"), "Pass TDs" (not "Passing TDs"), and space-separated NBA combos; `MARKETS.WNBA` was `null` and NCAAF/NCAAB had no table. Measured live: 731 of 1,448 lines unmapped before, 72 after. Mapping now prefers the provider's stable key (`PROVIDER_MARKET_KEYS`, e.g. `player_reception_yds`) with the display label as fallback, threaded shell → frontdoor (`marketId`) → `researchPlayerProp({ providerMarketKey })`. Add new markets to `PROVIDER_MARKET_KEYS`, not just to the label table.
  - Deliberately still unmapped: `player_points_q1` (quarter markets cannot come from full-game logs), `player_kicking_points` (needs weighted scoring the summing model cannot express), `player_tds_over` (ambiguous). Do not "fix" these by mapping them to a nearby stat.
  - **Line movement history did not exist.** All 1,274 snapshots were one burst — `longest_tracked_span` 00:00:00 — because persistence only ran on a visitor cache miss and `warmSports()` runs once. `lib/autoscout/ingest-worker.mjs` now snapshots on a schedule (default 20m, floor 5m, 96 cycles/day cap, idle leagues re-checked every 6th cycle, forces cache bypass, idle without a database). State at `/api/apex/diagnostics`.
  - `line_snapshots` has a unique index on `(prop_id, bookmaker_key, side, line, price, provider_updated_at)`, so unchanged lines are not re-recorded. Treat that table as a change-log, not a poll log.
  - The database is NOT the blocker: it is connected in `secure-rpc` mode and holds real rows. `persistenceHealth().lastWriteAt` is per-process memory, so it reads `null` in the frontdoor even while the core is writing — do not read that as "not connected".

- 2026-09-10: ChatGPT resumed the data-foundation work on `chatgpt/research-foundation-phase3` after reviewing Claude's current reference branches (`claude/research-pipeline-reference` and `claude/supabase-auth-standalone-reference`). Claude's research findings are treated as reference, not blindly merged, because production has advanced to the ClearSports/v5 architecture. Supabase persistence was connected in Railway through the existing token-gated `autoscout_ingest_board`/`autoscout_line_history` RPC contract. The database functions were repaired to include the `extensions` schema in `search_path` so `digest()` resolves correctly. Cached boards are backfilled into canonical events/players/props/lines without inventing line-history snapshots, while fresh provider boards create real snapshots. A low-cost periodic persistence pass is added to build line history over time. Historical L5/L10/L15 still require a true per-game stats provider; ClearSports season totals must never be presented as game logs or projections.
- 2026-09-10: Claude Code session. **Two board schedulers now exist. Run one, never both.**
  - ChatGPT's `frontdoor-clearsports.mjs` persists every `AUTOSCOUT_INGEST_MINUTES` (default 360) without forcing a refresh, and skips snapshots on a cached board so no synthetic history is written. That one is ACTIVE.
  - Claude's `lib/autoscout/ingest-worker.mjs` is OFF unless `AUTOSCOUT_INGEST_ENABLED=true`. It forces a fresh fetch — which is what actually accumulates line movement — and adds a daily cycle budget and idle-league skipping. Turn the other one off before enabling it, or the odds provider is billed twice for the same rows.
  - Deployed to `production-stable` by owner instruction after `npm run check` passed (284 tests). Contains the market-mapping fix and the customer-facing vendor/credit sanitizer.

- 2026-09-10: ChatGPT HIGH completion audit on isolated `chatgpt/autoscout-completion-audit`, based on deployed `25dec72431fc84fefbab70106424ed44c25cfebc`. Application/production/database unchanged. See `docs/autoscout-completion-audit-2026-09-10.md` for evidence, architecture decisions and Medium roadmap. Baseline `npm run check`: 295 passing. Live NFL lines and persisted snapshots verified; sampled research still season-only/unavailable. Preserve Claude's newer game-log/mapping/sanitizer work. Prioritize honest season coverage, unified calculations/cache/filtering, isolated history series, sport-switch races and server-bound saves. Existing account handlers are not currently wired in production `server-scout.mjs`; do not assume Supabase tables mean end-user auth is integrated. Do not activate both ingestion schedulers or accept pre-existing staged Railway variable changes. Separate uncommitted v5 frontend work in `ui/clearsport-research-polish` and the unintegrated Astra polish branch were left intact. User explicitly requires a STOP now until MEDIUM + CONTINUE, and another STOP before LOW.

- 2026-09-10: ChatGPT MEDIUM implementation candidate on the same isolated branch, **not deployed**. See `docs/autoscout-medium-handoff-2026-09-10.md`. Shared research calculations and bounded provider caches added; existing filter engine extended; v5 research board/drawer/mobile cards, book comparison and isolated persisted-history display implemented. `/api/saved-props` now binds atomic mounted-volume saves to the existing access-profile subject; it does not masquerade as Supabase Auth or populate incompatible user foreign keys. The UI explicitly describes shared-code save semantics. The frugal scheduler moved into the data core and is mutually exclusive with Claude's enabled ingest worker; no frontdoor scheduler remains in this candidate. Providers, hard qualifiers and database permissions were preserved. `npm run check`: 304 passed. Actual auth/saved and frontdoor HTTP tests passed. Browser preview starts but rejects a required module with `ERR_BLOCKED_BY_CLIENT`; desktop/mobile QA and live deployment validation remain open. Stay in Medium; do not claim readiness or switch to Low yet.

- 2026-09-10: Medium follow-up: refreshes retain the visible same-sport board, mark failed refreshes stale, and time out; obsolete sport responses remain excluded. Saved snapshots are explicitly labeled in rows/drawers. Drawer/row focus is restored after research updates; sport tabs retain their DOM identity. Relevant research/save tests and JavaScript syntax checks pass; browser QA remains pending.

- 2026-09-10: Medium context fix: failed/empty SportsDataIO logs preserve independently supplied player context and matchup identity, while historical statistics remain unavailable. ClearSports/fallback context merges retain real zero/false values and do not overwrite supplied values with null. Removed obsolete duplicate calculation helpers. Sixteen targeted research tests pass.

- 2026-09-10: Medium history follow-up: added an observation-only history chart and explicit current-offer comparison, with single-timestamp building states, null-safe change calculation and scoped series tests. Research retry remains subject to existing server caches/cooldowns. Eight relevant tests and UI syntax pass; visual verification remains open.

- 2026-09-10: Medium integrity/context follow-up: SportsDataIO historical rows now exclude known unfinished, future and did-not-play records while retaining genuine zero results. Drawer context displays supplied position/injury information, opponent, sampled home/away averages and L5-vs-L10 change only with ten games. Generated runtime files and access-profile save files are excluded from git. Targeted data tests pass.

- 2026-09-10: Implementation checkpoint through `e20983c`: full suite now 308 passed, zero failures; working tree clean before this note. Concise Claude review scope appended to the Medium handoff. Implementation remains local; browser and production release gates are open.

- 2026-09-10: Saved view now shares one current-plus-archived group set for board, filters and drawer markets; opening Saved reloads server saves across devices. Selected filters remain visible when no matching options remain. UI syntax and two saved-state/security tests pass. Git push is blocked by missing workspace GitHub credentials, not browser QA; no deployment or merge attempted.

- 2026-09-10: User explicitly requested integrating Astra into the live site. Recovered local completion candidate `4861d69bfc18e6d5cf4af15274a1bd830f45c7b3` from the build workspace; it already contains deployed `25dec72`. Release integration preserves all candidate files and existing production fixes. `npm run check`: 308 passed. Publishing through connected GitHub because terminal Git credentials are unavailable. No staged Railway variables are to be accepted. Browser preview remains unverified; deployment and live verification results will be recorded separately.

- 2026-09-10: Astra release PR #22 merged as `628f1950c1edf6c3fca96b4a767fc468e615d2cb`; both GitHub release checks and public production smoke passed on candidate `4d90c7e`. Actual frontdoor HTTP checks passed for shell, all shared modules/CSS, health, and anonymous saved-props 401. The complete Astra application snapshot is now on production-stable. Railway rollout remains pending: ordinary redeploy reused old 25dec72, and the Railway agent is unavailable due to its usage limit. This handoff commit emits a source update for the existing production branch; do not apply staged environment changes. Verify Railway source commit and live browser before declaring the candidate live.

- 2026-09-10: Phase 2 redesign candidate published on `chatgpt/autoscout-paid-research`, based on `fc93118`. Retains Astra's application and provider contracts. Adds readable responsive shell, persistent per-sport filters, optional Rules switch, column controls, explicit research batches, tabbed player details and safer saved-list/sign-out state. Fixes prefixed team matching (`nfl_lar`/`LAR`) and dropped ClearSports injury context when logs are missing. 309 tests passed; no browser/release-completion claim. See `docs/autoscout-redesign-handoff-2026-09-10.md`. Owner requested High review after implementation, then Medium corrections before deployment.

- 2026-09-11: Public NBA/WNBA game history added on `chatgpt/autoscout-public-basketball-logs`, based on deployed `3996c07`. User requested a no-subscription source. Both public basketball game-log feeds returned HTTP 200 inside Railway. A cached exact-name/league resolver and stat-name adapter supplement ClearSports context without changing The Odds API quotes, auth, saved props, ingestion, or disabled SportsDataIO calls. Shared research calculations handle zero/push/sample semantics. Preseason, All-Star, future, DNP and unknown-value records are excluded; unsupported markets and incomplete season coverage remain unavailable. Captured-response tests and targeted research tests passed (23). No claim of contractual availability or projections from this public feed. Deployment must verify the new commit and actual API/UI result.

- 2026-09-11: User expanded public ESPN history to NFL/NBA/MLB/NHL/WNBA/NCAAF on `chatgpt/autoscout-six-sport-history`, based on `67b34b1`. Those six research routes now use only the public adapter, without paid-stat fallback. Sport-specific fields, MLB batting/pitching isolation, NFL regular/postseason, scores/dates, full returned-season arrays and ID-based H2H are covered by 28 targeted tests. Previous-season history remains separate from the labeled season card. UI hides absent context and labels calculated average minutes honestly. Keys, odds offers, auth/saves, ingestion and Railway settings remain untouched. Release workflow and deployed commit/UI verification required before completion.

- 2026-09-11: Claude Code session on `claude/obligepay-credit-saving-mode-qsinxn`, rebased onto `production-stable` (`3531680`). **Verification only — no application code, provider, schema, billing, API-key or Railway setting was changed.** This closes the open gate left by the two 2026-09-11 entries above ("Deployment must verify the new commit and actual API/UI result").
  - **Deployed commit verified.** Railway deployment `b99d039f-ddec-4d65-9854-af2dcaff5ec2`, status SUCCESS, `commitHash 3531680b05d890738796812a9e37d8d2afce8856`, branch `production-stable`, deployed 2026-09-11T01:06Z. `www.obligepay.com` and `autoprop-live-production.up.railway.app` report the same `startedAt`, so the public domain and the Railway domain serve the one deployment. The earlier "still source `25dec72`" note in `docs/autoscout-redesign-handoff-2026-09-10.md` is now stale.
  - **Live API verified** (public, unauthenticated): `/api/health` and `/api/apex/health` 200 and sanitized — provider identity reads `Live odds`, no vendor name, credits or plan limits. `/api/apex/research-health` returns readiness only. `/api/apex/props` returns live boards: NFL 830, WNBA 96, NBA 78, MLB 57, NCAAF 18; NHL and NCAAB return 0 props (no scheduled games, not a defect).
  - **Public six-sport history verified live, with real numbers.** Sampled `/api/apex/research`: NFL/NBA/WNBA available; MLB partially available. Puka Nacua / Reception Yards 63.5 returned `source: "Historical stats"`, `coverage.gamesReturned 32`, `seasonComplete true`, per-game rows carrying real opponent, score, result and stat values with `push`/`hit` flags, and computed L5/L10/L15/L20/season windows plus home/away splits. NCAAF research is unavailable because `player_tds_over` is deliberately unmapped.
  - **Browser QA performed and PASSED** — this was the step blocked in every prior handoff. Astra's `ERR_BLOCKED_BY_CLIENT` did not reproduce. Chromium could not reach the site through this session's egress relay directly (tunnels reset), so the browser was pointed at a local read-only forwarder to the live site; the deployment itself was unmodified.
    - Desktop 1440x900: board renders the navy workspace, sport tabs, filters, Columns/Rules controls, summary tiles and 40 rows with player artwork.
    - Research is opt-in by design: rows show `—` until "Research N props" is pressed, then real hit rates populate within ~10s (e.g. Aaron Rodgers Pass Attempts L5 80% / L10 70% / L15 60% / SZN 59%; Bijan Robinson Reception Yards H2H 100%).
    - Drawer opens from a row (click and Enter/Space both work; `article.asRow` carries `tabindex=0` and an aria-label). It renders Overview / Game log / Compare lines tabs, the line stepper, OVER/UNDER, hit-rate windows, ALL/HOME/AWAY/VS-opponent filters and a 10-bar game chart with Hit/Miss/Push legend and threshold line.
    - iPhone 13 (390px): no horizontal overflow before or after research, research batch works, same values, zero page errors.
    - Only HTTP >= 400 observed anywhere: `401` on `/api/saved-props` for an anonymous visitor, which is the documented behavior.
  - **Open items (not fixed here, no code touched):** (1) the board's Season column shows `59% / 31.2 avg · 17 games` with no season year, while the drawer correctly labels the same figure `17g · 2025` — on a 2026 board the unlabelled column reads as current-season; (2) every sampled NCAAF prop is a `TDs Over` market, so 100% of the visible NCAAF board can never return research; (3) `/api/apex/research` requires `playerName`, not `player` — a bare `player` param returns `400 INVALID_RESEARCH_REQUEST`.
  - **Missing checkpoint reported, not recreated:** commits `d3d14c6` and `e20983c` (the "308 tests passed" Medium checkpoint) and local candidate `4861d69` do not exist on `origin` or in any of the 54 remote branches; branch `chatgpt/autoscout-completion-audit` was never pushed. Per the 2026-09-10 PR #22 entry that work was republished through `628f195`, and its files (`lib/autoscout/saved-props.mjs`, `lib/analytics/research.mjs`, `apex-v2/scout-ui-v5.js`, the saved-props tests) are present on `production-stable`. Only the original SHAs are unreachable. Nothing was rebuilt.


- 2026-09-12: Multi-sport pipeline repair candidate on `chatgpt/multisport-log-pipeline-fix`, based on production `3a470ad8`. Public-history field/entity contracts now distinguish QB sacks taken, individual defensive sacks, verified team units, MLB batting/pitching, NHL goalie/skater, college basketball and soccer competitions. Previous regular-season history fills rolling samples but never SZN. Research hit rates use hits/all eligible games and pushes end its active-pick streak; the legacy scanner's default push/qualification policy is unchanged. Visible-page cards hydrate automatically in small batches with skeletons, retry states and honest N/A reasons. No paid provider credentials, authentication, billing, DB permissions or Railway settings were changed; new soccer leagues are on-demand only, not added to automatic paid polling. Local targeted calculation/parser tests pass (47); full release, real-source integration and desktop/mobile browser gates must pass before promotion. Soccer passes/tackles, unfamiliar schemas and unsupported markets remain unavailable when the source has not actually supplied them. Do not replace absence with zeros or relabel a season sample as complete.

## 2026-09-12 Main sportsbook / Ask / projected-stat owner request
Work branch: chatgpt/sportsbook-home-projections-tacos, based on e91bb5a.
Owner requested main game-market UI on ObligePay, projected stat, Gemini-capable Ask,
Taco section and broader available book coverage. See docs/sportsbook-main-2026-09-12.md.
Regular Auto Scout still excludes promotions and alternates. Tacos use a separate
explicit-metadata-only channel; live ingestion is unverified (public source HTTP 403).
Existing provider caps, key values, auth contracts and single ingestion scheduler
remain authoritative. Never describe the calculator as a wager-acceptance backend.
