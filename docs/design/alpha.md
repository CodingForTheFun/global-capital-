# Alpha — saved design baseline

User name assigned on 2026-09-20: **Alpha** means the ObligeProps frontend design immediately before PR #452, not the subsequently rejected design.

- Permanent repository branch: `design/alpha`
- Exact checkpoint: `dd977859dd7aae3ccbdb751b35c318000648b8ee`
- Frontend: `apps/oblige-web`

If the user says “restore Alpha”, restore the visual presentation from this checkpoint through a reviewed frontend change. Keep current backend data accuracy, authentication and release controls; do not reset production branches or redeploy an obsolete backend. Compare presentation files against this checkpoint to prepare the restoration.

The replacement direction uses the user's third reference for player cards (portrait, colored history tiles, book prices), first for player research (compact identity, filters, chart), and second for desktop navigation. Sample figures in the reference pictures are visual examples only and must never be used as live statistics.
